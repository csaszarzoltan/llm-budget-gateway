# Migration to 2.0.0

The release is additive. Existing APIs and databases remain unchanged. Configure `GATEWAY_PLATFORM_API_KEY` before enabling the Platform API. The twenty MVP capabilities are stateless; future shared repositories can be added behind adapters without changing the domain contracts.
