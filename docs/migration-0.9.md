# Migration to 0.9.0

This additive release leaves proxy, control, intelligence and operations contracts unchanged. Configure `GATEWAY_EVALUATION_API_KEY`; otherwise protected quality routes return 503. Run the app on port 8004. Back up `evaluations.db` before moving hosts.
