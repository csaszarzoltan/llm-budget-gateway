# Migration to 0.7.0
The release is additive. Existing gateway routes and databases are unchanged. Run the Intelligence app separately on port 8002. Set a non-default webhook secret when constructing the app. Back up the SQLite file before moving between hosts.
