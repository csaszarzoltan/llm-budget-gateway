# Gateway Homepage 7.3.1

The core Gateway now exposes a responsive homepage directly at `http://localhost:8000/` and `http://127.0.0.1:8000/`.

The page links to OpenAPI documentation, model listing, health status, OpenAPI JSON and Unified Console. It also highlights the primary chat, embedding and model endpoints. Existing API routes are unchanged.
