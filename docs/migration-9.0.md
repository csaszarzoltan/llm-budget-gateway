# Migration to 9.0.0

This release is additive and stateless. Existing APIs, databases, service homepages and console controls remain unchanged. Configure `GATEWAY_ADOPTION_API_KEY` only when enabling Product Adoption Center on port 8017. No database migration is required.
