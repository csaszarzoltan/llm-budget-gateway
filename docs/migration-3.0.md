# Migration to 3.0.0

This release is additive. Configure `GATEWAY_AGENTOPS_API_KEY`. Existing APIs and databases are unchanged. Domain services are stateless; production persistence can be added behind adapters.
