# Migration to 4.0.0

Additive release. Configure `GATEWAY_FLEET_API_KEY`. Existing APIs/databases remain unchanged. Domain services are stateless; shared persistence can be introduced behind repositories.
